# Review Queue

Human-in-the-loop items found during sync/ingest/lint: contradictions, uncertain claims,
coverage gaps. Claude appends items instead of guessing; the human resolves or delegates.

Format: `- [ ] YYYY-MM-DD — <description> (found during <workflow>; pages: page-slug, other-slug)`

> Resolution strategy for the six open 2026-07-05 items (options, pros/cons,
> recommendations, sequencing): `docs/plans/review_items_resolution_strategy.md`
> (items R1–R6, in file order below).

- [x] 2026-07-04 — `.claude/CLAUDE.md` §ven-architecture cites `docs/plans/ven_backend_architecture_refactoring.md` as its reference, but that file no longer exists in the working tree. Update or remove the reference. (found during seed sync; pages: ven-hexagonal-architecture) — **RESOLVED 2026-07-04**: reference now points to `docs/architecture/VEN_ARCHITECTURE.md` + module dependency graph.
- [x] 2026-07-04 — `docs/REQUIREMENTS.md` §2.3 still defines the Planner as using "a greedy algorithm"; superseded by the two-phase MILP. Glossary needs updating. (found during seed sync; pages: milp-over-greedy, milp-planner) — **RESOLVED 2026-07-04**: glossary entry now names the two-phase MILP solver (HiGHS) on the 3-tier grid.
- [x] 2026-07-04 — purpose.md asks for use cases *implied by the OpenADR 3 spec* to be systematically derived and gap-checked against the code base. (found during seed sync; pages: system-use-cases, vision-and-roadmap) — **RESOLVED 2026-07-04**: first pass written as [[openadr-spec-use-cases]] from User Guide §5–§7 + cert backlog; deepen later via `/wiki-ingest docs/openadr_3_1_specs/`.
- [x] 2026-07-04 — `docs/REQUIREMENTS.md` §2.3 "Energy Packet" glossary entry describes packet-based scheduling as current, but the code removed its last remnants (PacketSeed config, `packet_id` fields) on 2026-07-04 — device sessions carry the lifecycle now; only comment vocabulary and `PacketTransition` trace events remain. Decide: rewrite the glossary entry as historical, or rename residual code vocabulary. (found during review-item fixes / wiki-query; pages: hems-planning) — **RESOLVED 2026-07-04**: `ControllerEvent::PacketTransition` was confirmed dead (never constructed by any code path; the energy-target-by-deadline concept it once described lives on fine in `EvSession`/`HeaterTarget`/`ShiftableLoad`) and removed rather than renamed — `VEN/src/controller/trace.rs` (enum variant), `VEN/src/controller/reporter.rs` (match arm + doc comments), and the matching `VEN/ui` rendering/test support (`api/types.ts`, `TraceTable.tsx`, `PlanTriggerTimeline.tsx`, `PlanTriggerTimeline.test.tsx`) were all updated together. `docs/REQUIREMENTS.md`'s glossary entry itself is unchanged — still describes the concept correctly (energy-by-deadline), just no longer names the retired `PacketTransition` trace event. **FOLLOW-UP 2026-07-04**: glossary entry renamed **Energy Packet → Device Session** and rewritten to describe the actual per-asset structs (`EvSession`/`HeaterTarget`/`ShiftableLoad`, `entities/device_session.rs`) instead of the generic `PENDING→ACTIVE→COMPLETED/ABANDONED` lifecycle. `hems-planning.md`'s DRIFT callout removed (no longer a drift) and folded into a plain glossary cross-reference in the Accounting section.
- [x] 2026-07-05 — `.claude/CLAUDE.md` §ven-architecture lists `SolverPort: services → controller/milp_planner (solve)` as a port obligation, but no such trait exists (`tasks/planning.rs` calls `run_planner()` directly). Human-owned file — decide: introduce the port (a `dyn SolverPort` seam would also let `services` own the planning cycle instead of `tasks/`) or amend the rule to name `AssetMilpContext` as the solver-side port. Same file's §testing says shared mocks in `services/test_support/` are "not cfg(test)" — they are `#[cfg(test)]`-gated. (found during VEN code ingest; pages: ven-hexagonal-architecture, ven-code-vs-docs-audit) — **RESOLVED (R1, Option A)**: `controller/solver_port.rs` introduces the real `SolverPort` trait + `SolveRequest`; `MilpSolver` (`milp_planner/mod.rs`) implements it; `services::PlanningService::solve_plan` is the only caller. `.claude/CLAUDE.md`'s `#[cfg(test)]` wording also corrected.
- [x] 2026-07-05 — `docs/architecture/VEN_ARCHITECTURE.md` has accumulated stale sections that now contradict the code: §2.1 dispatcher description (auto-follow / deviation distribution — not implemented; ledger lives in monitor), §2.1 event table (ALERT/DISPATCH_SETPOINT/CHARGE_STATE_SETPOINT/export-subscription handling absent), §2.2 "20 s periodic" (default is 300 s), §2.3 StaleRatePolicy (dead enum), §3.0 `AssetInterface`/`SimulatedAsset`/`MeasuredAsset` (never built — real shape is `Asset` trait + `AssetConfig` + `AssetHandle`), §3.3/§4.5/§4.7 `/trace` + `/sim/override` (endpoints replaced by `/trace/events`+`/trace/history` and `/sim/inject`), §4 route list (registered in `routes/mod.rs`, ~14 endpoints missing), §5.2 audit (describes pre-TimeSeries code; `common/mod.rs` now implements most of §5.3's "target"). Outside wiki write scope — needs a doc rewrite pass; wiki DRIFT callouts are the interim record. (found during VEN code ingest; pages: ven-code-vs-docs-audit, dispatcher, openadr-interface, asset-layer) — **RESOLVED (R2, Option B)**: two-stage rewrite of `VEN_ARCHITECTURE.md` done in one pass, with every gap tagged `❌ GAP` citing its tracking item (never presented as settled fact) — the wiki DRIFT callouts this item pointed at are now resolved as a side effect (the docs self-correct instead).
- [x] 2026-07-05 — Plan-cycle TELEMETRY_STATUS reports are dead code: `tasks/planning.rs:338` passes `program_id = None` and `build_status_report` returns `None` without one. Decide: resolve a real programID (e.g. from the first active event, as tests do) or delete the call path + reporter branch. Note `docs/BACKLOG_OpenADR_Cert.md` §reports rates this row "Full — Triggered on `PlanCycle` controller event", which is therefore wrong too. (found during VEN code ingest; pages: openadr-interface, ven-code-vs-docs-audit, openadr-spec-use-cases) — **RESOLVED (R3, delete)**: `build_status_report` and its call site removed, plus cascading dead code (helper function, dead struct field, dead params). `docs/BACKLOG_OpenADR_Cert.md`'s row corrected to "Missing", noting `/trace/events`/`/plan/events` as the surviving observability paths.
- [x] 2026-07-05 — File-size rule vs reality: nine `VEN/src/` files exceed the 500-line cap even counting only production lines (heater 799, profile 777, assets/mod 687, routes/hems 678, ev 634, reporter 559, battery 523, state 519, simulator/mod 503) and `tasks/planning.rs` is 363 vs the 200 cap. Decide: split (suggested cut lines in [[ven-code-vs-docs-audit]]) or amend the rule; note CI that would enforce this is not configured yet. (found during VEN code ingest; pages: ven-code-vs-docs-audit, ven-hexagonal-architecture) — **RESOLVED (R4)**: the rule is defined as *production lines* (non-blank, excluding `#[cfg(test)]` blocks and test-only paths), measured by `scripts/audit_file_sizes.py`, which runs in CI (file_size_audit workflow) and passes repo-wide; `assets/mod.rs` is the one documented allowlist entry (its fix is the enum→trait refactor, R-08 in the refactoring backlog).
- [x] 2026-07-05 — Dead-code inventory awaiting a keep/wire/delete decision: `StaleRatePolicy` enum, `apply_battery_correction_overlay` (tested, unwired), `DomainError::{PlanInfeasible,VtnUnreachable,ProfileInvalid}`, `HvacService`, and the `#![allow(dead_code)]` spec-vocabulary blocks in `entities/asset.rs` (`AssetProfile`, `AssetHeuristics`, `AssetForecast`, `AssetLedger`, `PenaltyRule`, …) and `entities/capacity.rs` (`OadrEventCache`, `OadrProgramConfig`, `OadrCapacityRequest`). These blocks are the root cause of two doc drifts (readers cite them as behaviour). Per project rules this belongs in `docs/reference/TECHNICAL_DEBTS.md` — wiki workflows don't write there, so transfer manually. (found during VEN code ingest; pages: ven-code-vs-docs-audit, reliability-and-config, tariffs-and-capacity) — **RESOLVED (R5, Option B — quarantine, not delete)**: the user confirmed the vocabulary block is wanted future roadmap, not abandoned — moved verbatim to new `entities/design_vocabulary.rs` with a not-current-behaviour banner (`ComfortRate` corrected out of the dead list — it's actually live). `apply_battery_correction_overlay`/`HvacService`/`OadrEventCache` family/`DomainError` variants also kept (not deleted) with corrected comments. Every item now has a `docs/BACKLOG.md` entry (BL-14 through BL-29, including 7 more zero-reference types found during implementation: `AssetState`, `PowerAdjustability`, `UserRequestMode`, `FlexibilityDirection`, `RateType`, `RateUnit`, `PowerRange`).
- [x] 2026-07-05 — Report obligations are one-shot: `extract_report_obligations` dedups by (event, payloadType) and `mark_obligation_fulfilled` never re-arms, so `reportDescriptor.frequency: 900` yields one report instead of one per 15 min. Certification-relevant; decide whether to implement recurring obligations. (found during VEN code ingest; pages: openadr-interface, openadr-spec-use-cases) — **RESOLVED (R6, Option A — recurring re-arm)**: `state.rs::mark_obligation_fulfilled` replaced with `rearm_obligation` (advances `due_at` by `interval_duration_s` instead of permanently disabling); new `retire_obligations_not_in` prunes an obligation once its source event expires. Report windowing and VTN upsert naming needed no change — both already matched the recommended "bounded trailing window, stable name" shape once obligations could actually recur.
- [x] 2026-07-06 — Coverage gap: no wiki page explains the OpenADR **Program** concept at the business level (what a program is, why VTN/BL offers one, why a VEN enrolls, enrollment being out-of-band/contractual per User Guide §6.1–§6.4, one-event-one-program rule, multi-program enrollment, open no-credential tariff programs). [[openadr-3]] and [[openadr-spec-use-cases]] mention programs only in passing; a `/wiki-query` on this had to answer mostly from `docs/openadr_3_1_specs/3_OpenADR 3.1.0_User_Guide_20250801.md` directly. Consider a `concepts/openadr-programs.md` page. (found during wiki-query; pages: openadr-3, openadr-spec-use-cases) — **RESOLVED 2026-07-06**: [[openadr-programs]] created by promoting the filed query [[openadr-programs-explained]] (all five worked examples kept per user request); [[openadr-3]] links to it; the query page now points forward to the concept page.
- [x] 2026-07-04 — [[openadr-interface]]'s outbound-obligations table states `USAGE_FORECAST` is reported with "FIRM slots: point forecast; FLEXIBLE slots: range [0, MaxPower]" (sourced from `docs/architecture/VEN_ARCHITECTURE.md` §2.1, an architecture/design doc). Verified against `VEN/src/controller/reporter.rs`: no code path builds a `USAGE_FORECAST` payload — the only literal payload types emitted are `USAGE`, `OPERATING_STATE`, `STORAGE_CHARGE_LEVEL`, `IMPORT_/EXPORT_CAPACITY_RESERVATION`, `TELEMETRY_STATUS`. `USAGE_FORECAST` appears nowhere outside a comment in `entities/capacity.rs`. The architecture doc describes a target design that was never implemented, and the wiki inherited the claim without checking the reporter code. (found during wiki-query on energy-packet/forecast-reporting; pages: openadr-interface, openadr-spec-use-cases) — **RESOLVED 2026-07-04**: `openadr-interface.md` corrected to list only the payloads actually built, with a DRIFT callout explaining the gap and pointing to §8.7/§8.8 of the spec; `hems-planning.md` softened its own restatement of the same false claim; `openadr-spec-use-cases.md`'s Capability/Operational Forecast Reporting rows changed from 🟡 to ❌ with the real root cause (`reportDescriptor.historical` is never parsed). `docs/architecture/VEN_ARCHITECTURE.md` §2.1 itself was left untouched — outside the wiki's write scope; the DRIFT callout is the durable record.
- [x] 2026-07-17 — Coverage gap: [[ven-ui]] names the Planner tab only as "planner timeline views" — it doesn't describe the page's actual composition (`VEN/ui/src/pages/Planner.tsx`: objective selector + weight legend, SSE PlannerStatusBar, PlanHeaderBar, PlanPowerStack, PlanTriggerTimeline, PlanDecisionMatrix, PacketProgressBoard, collapsible TraceTable, CorrectionBanner) or its role split (user controls vs. diagnostic surfaces). A `/wiki-query` on the tab's purpose had to answer from the code directly. Extend [[ven-ui]] with a per-page section or at least a Planner-tab paragraph. (found during wiki-query; pages: ven-ui) — **RESOLVED 2026-07-17**: [[ven-ui]] gained a "Planner tab" section covering the full composition and the new SessionProgressBoard (f068d94).
- [ ] 2026-07-17 — CONTRADICTION + dead UI: `PacketProgressBoard` (`VEN/ui/src/components/planner/PacketProgressBoard.tsx`) is a leftover of the removed EnergyPacket abstraction (Phase D, commits efd861f/b63b160/b973da6/0079a77, resolved review item of 2026-07-04). The UI still ships `EnergyPacket`/`PacketStatus` types, `usePackets()` (10 s polling) and `api.packets()` → `GET /packets`, but no such route exists anywhere in `VEN/src/routes/` — the request 404s every poll, react-query stays in error state, and the board permanently renders "No energy packets." Yesterday's [[planner-tab-purpose]] query page classifies the board as one of the tab's two genuinely *user-facing* elements and BL-36 (docs/BACKLOG.md) proposes surfacing it on the Dashboard — both built on a component that can never show data. Decide: (a) delete the board + usePackets + EnergyPacket types as dead code, or (b) rebuild it on the live vocabulary (UserRequest + EvSession/HeaterTarget/ShiftableLoad via `/user-requests`), which is what BL-36 actually wants. Correct [[planner-tab-purpose]] and BL-36 accordingly. (found during wiki-query; pages: planner-tab-purpose, ven-ui, hems-planning) — **RESOLVED 2026-07-17 (option b — rebuild)**: user decided against reviving EnergyPacket (the deleted part was the lifecycle state machine, not the question); board rebuilt UI-only as `SessionProgressBoard` on `/user-requests` + live sim snapshot + plan envelopes/`planned_kw_by_asset`, budget bar honestly labeled "est." (accumulated cost spun off as BL-39). BL-36 done in the same change (Dashboard `dash-session-strip` + objective chip) and removed from BACKLOG.md; whole packet surface (types, `usePackets`, `api.packets()`, stale `FlexibilityEnvelope.packet_id`) deleted from the UI. [[planner-tab-purpose]] corrected with a dated correction section. Commit f068d94.
- [ ] 2026-07-16 — Stale-page remainder from the scoped 2026-07-16 sync: ~30 pages carry `synced_commit` older than changes to their sources (mostly docs edited during the total-review C2 rewrites, spec-file source lists, and import-path-only code renames — see the 2026-07-16 log entry for the scoping rationale). Work through `bash scripts/wiki_lint.sh`'s STALE list: re-verify each page against its sources and bump `synced_commit`, or correct content where the C2 doc rewrites changed a cited claim. (found during /wiki-sync; pages: see lint output)
