Feature: Simulation tab override switch UI

  # Scenarios that previously tested reactor mode (CHARGE_SETPOINT) have been
  # removed as part of speckit 004 (VEN Controller Reform). The reactor and its
  # FSM modes are gone. UI override scenarios for the planner-driven control path
  # will be added in speckit 003 (UI reform).
