# Tasks: Remove Profile from Routes Layer (AB-06)

**Input**: Design documents from `/specs/023-remove-profile-routes/`  
**Branch**: `023-remove-profile-routes`  
**Prerequisites**: plan.md ✅, spec.md ✅, research.md ✅, data-model.md ✅, quickstart.md ✅

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (US1, US2, US3)

---

## Phase 1: Setup

**Purpose**: Capture pre-change baseline before any code is modified.

- [X] T001 Create `VEN/tests/fixtures/` directory (if absent). The fixture `schema_snapshot.json` is generated by the golden-file snapshot test (T003) on its first run — if the file does not exist, T003 writes it and passes; subsequent runs assert byte-match. **No running VEN instance is required.** This task is complete once the `fixtures/` directory exists.

---

## Phase 2: Foundational (Blocking Prerequisite)

**Purpose**: Visibility change required before integration tests can compile.

**⚠️ CRITICAL**: T003 and T008 cannot compile until this is done.

- [X] T002 Change `schema_from_profile` visibility from `pub(crate)` to `pub` in `VEN/src/simulator/mod.rs` (line 419) — required so `VEN/tests/` integration crates can call it

**Checkpoint**: `cargo build` must still pass after T002.

---

## Phase 3: User Story 1 — Route Handlers Are Config-Agnostic (Priority: P1) 🎯 MVP

**Goal**: `GET /sim/schema` serves pre-computed schema from `AppCtx.sim_schema`; `routes/sim.rs` no longer imports or touches `Profile`.

**Independent Test**: `cargo test` — schema snapshot test green; `grep -r "use crate::profile" VEN/src/routes/` returns empty.

### Tests for User Story 1

> **Write test FIRST — schema snapshot test should pass both before and after; it guards against regression**

- [X] T003 [P] [US1] Write schema snapshot integration test in `VEN/tests/schema_snapshot.rs`: load `VEN/profiles/ven-1.yaml` using `env!("CARGO_MANIFEST_DIR")` to resolve the path, call `ven_app::simulator::schema_from_profile`, serialise to JSON, assert it byte-matches `VEN/tests/fixtures/schema_snapshot.json`

### Implementation for User Story 1

- [X] T004 [P] [US1] Add `sim_schema: Arc<HashMap<String, Vec<ControlDescriptor>>>` field to `AppCtx` struct in `VEN/src/main.rs` (after the `profile` field, around line 44)
- [X] T005 [US1] Pre-compute `sim_schema` at application startup in `VEN/src/main.rs`: call `simulator::schema_from_profile(&profile)`, wrap in `Arc::new(...)`, assign before `AppCtx` construction (depends on T004)
- [X] T006 [US1] Update `get_sim_schema` handler in `VEN/src/routes/sim.rs` (line 116–120): replace `crate::simulator::schema_from_profile(&ctx.profile)` with `(*ctx.sim_schema).clone()` (depends on T004, T005)
- [X] T007 [US1] Verify `cargo build` and `cargo test` pass; confirm schema snapshot test is green; confirm `grep -r "use crate::profile" VEN/src/routes/` returns zero matches (depends on T003, T006)

**Checkpoint**: US1 complete. Route layer is profile-free. Schema identity confirmed by test.

---

## Phase 4: User Story 2 — Automated Boundary Check Prevents Regression (Priority: P2)

**Goal**: A `#[test]` in `VEN/tests/architecture.rs` permanently enforces that no file under `VEN/src/routes/` imports `crate::profile`. The test fails when a violation is present and passes when routes are clean.

**Independent Test**: `cargo test architecture` — passes on clean routes; fails when a test-injected violation is present.

### Implementation for User Story 2

- [X] T008 [US2] Write `VEN/tests/architecture.rs`: implement `#[test] fn routes_must_not_import_profile()` that walks all `.rs` files under `VEN/src/routes/` using `std::fs::read_dir` (no new deps), reads each file's content, and panics with a diagnostic message naming any file containing `use crate::profile` or `crate::profile::` (depends on T007 — routes must be clean first so test passes immediately)
- [X] T009 [US2] Manually verify SC-004: temporarily add `use crate::profile::Profile;` to `VEN/src/routes/sim.rs`, run `cargo test architecture`, confirm the test fails with a clear message naming the file, then revert the injection and confirm the test passes

**Checkpoint**: US2 complete. Boundary permanently enforced by the test suite.

---

## Phase 5: User Story 3 — Shared Context Field Is Explicitly Annotated (Priority: P3)

**Goal**: `AppCtx.profile` carries a doc-comment identifying `sim_tick` as its owner and deferring removal to Phase 4/5. The retention is intentional and visible.

**Independent Test**: Inspect `VEN/src/main.rs` — `profile` field has a doc-comment naming `sim_tick`.

### Implementation for User Story 3

- [X] T010 [US3] Add a `///` doc-comment above `pub profile: Arc<Profile>` in `AppCtx` in `VEN/src/main.rs`: `/// Retained for use by the sim_tick task. Removal from AppCtx is deferred to Phase 4/5 of the VEN backend refactoring.`

**Checkpoint**: US3 complete. All three acceptance stories satisfied.

---

## Phase 6: Polish & Cross-Cutting Concerns

- [X] T011 [P] Run constitution verifiable invariant: confirm `grep -r "use crate::profile" VEN/src/routes/` is empty — document result
- [X] T012 Push branch to remote, pull on Pi4-Server, run full BDD suite: `docker compose -f tests/docker-compose.test.yml run --build --rm test-runner` — confirm zero new failures (SC-003)
- [X] T013 Commit all changes with message: `refactor(ven): pre-compute sim schema at startup, remove profile from routes (AB-06)`

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — do immediately before touching any code
- **Foundational (Phase 2)**: Depends on Phase 1 — T002 must complete before T003 or T008 can compile
- **US1 (Phase 3)**: Depends on Phase 2 — T003→T004/T005 in parallel→T006→T007
- **US2 (Phase 4)**: Depends on Phase 3 (routes must be clean before the boundary test can pass)
- **US3 (Phase 5)**: Independent of US1/US2 — can run any time after Phase 2; placed last as it is P3
- **Polish (Phase 6)**: All user stories complete

### Within Each Phase

```
T001
  └── T002
        ├── T003 (write test)
        ├── T004 [P] (add field)
        │     └── T005 (pre-compute at startup)
        │           └── T006 (update route handler)
        │                 └── T007 (verify build + test)
        │                       └── T008 (boundary check test)
        │                             └── T009 (SC-004 injection verify)
        │                                   └── T010 (profile annotation)
        │                                         └── T011 [P], T012, T013
```

### Parallel Opportunities

- T004 (add field to AppCtx) and T003 (write snapshot test) can proceed in parallel after T002.
- T011 (grep invariant) and T012 (BDD run) are independent and can proceed in parallel after T010.

---

## Parallel Example: User Story 1

```
# After T002 completes, launch in parallel:
Agent A: T003 — write schema snapshot test
Agent B: T004 — add sim_schema field to AppCtx

# Once T004 done:
Agent A: T005 — pre-compute at startup
Agent A: T006 — update route handler

# Once T005 + T006 done:
Agent A: T007 — cargo build + test verify
```

---

## Implementation Strategy

### MVP (User Story 1 Only)

1. Complete Phase 1: capture fixture
2. Complete Phase 2: `pub(crate)` → `pub`
3. Complete Phase 3: add field, pre-compute, update route, verify
4. **STOP and VALIDATE**: constitution invariant passes, snapshot test green
5. US2 + US3 are additive hardening — not required for correctness

### Incremental Delivery

1. T001 + T002 → foundation ready
2. T003–T007 (US1) → route is clean, schema identity verified ← **ship-ready**
3. T008–T009 (US2) → regression guard in place
4. T010 (US3) → annotation complete
5. T011–T013 → full validation + commit

---

## Notes

- T001 **must** happen before any code change — the fixture captures pre-change output
- `schema_from_profile` is infallible (returns `HashMap` directly, no `Result`) — no `.expect()` needed
- `std::fs::read_dir` is sufficient for the architecture test — no `walkdir` crate needed
- Constitution invariant: `grep -r "use crate::profile" VEN/src/routes/` → empty after T006
- Existing BDD suite (FR-006/SC-003) is the acceptance gate — zero new failures required
