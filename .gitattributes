# Force LF for generated snapshot fixtures so Windows CRLF checkout never breaks the golden-file tests.
VEN/tests/fixtures/*.json text eol=lf
# Shell scripts must stay LF: docker_host_lock.sh pipes a heredoc to the Node1's Linux bash, which rejects CRLF.
*.sh text eol=lf
