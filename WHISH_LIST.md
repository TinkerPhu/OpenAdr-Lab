prepare for later pull requests for the openleadr-rs repo for each change/fix we make.
remember to fix the report query in the VTN. make sure that a VEN can never get information about other VENs - for any api call.
install gh CLI
make sure that the example events you added in the db as seeds can be re-created when cloning and setting-up the repo.
set up build chain in github
clean up docker orphans
The hashes are all different from what I computed! The issue is that I computed hashes on Windows, and the line endings differ.   Let me look at the actual query content to understand: